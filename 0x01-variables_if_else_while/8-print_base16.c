#include <stdio.h>

/**
 * main - main functions
 *
 * Return: always 0
 */

int main(void)
{
	int i;
	char j;

	for (i = 0 ; i < 10 ; 1++)
		putchar(i + '0');
	for (j = 'a' ; j <= 'f' ; j++)
		putchar(('\n');
		return (0);
}
